import { NgWhileDirective } from './ng-while.directive';

describe('NgWhileDirective', () => {
  it('should create an instance', () => {
    const directive = new NgWhileDirective();
    expect(directive).toBeTruthy();
  });
});
